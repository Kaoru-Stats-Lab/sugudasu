# `.cursor/rules/learned/` — Agent 手順専用

**製品・命名・デプロイ判断は書かない。** 正本は `docs/notes/` · `BACKLOG.md` · `.cursorrules`。

ここに置くものの例:

- Windows / PowerShell の実行順
- `npm run …` ゲートの固定順序（SSOT 未記載分のみ）
- 検証コマンドの最短セット

正本: `.cursor/rules/self-learning-sugudasu.mdc`
