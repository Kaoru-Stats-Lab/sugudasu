# Schedule 工程表 — 仕様 SSOT（提督回答確定 · v1）

**更新:** 2026-06-26  
**ステータス:** **提督回答反映 · 合意** — 実装正本（**完成後も提督判断で改訂可**）  
**起源:** [`schedule-spec-gemini-questions.md`](../prompts/schedule-spec-gemini-questions.md) 質問票 · 提督回答  
**関連:** [`SYNC_SCHEDULE_PRODUCT_DECISION.md`](SYNC_SCHEDULE_PRODUCT_DECISION.md) · [`SUGUDASU_SYNC_LINE.md`](SUGUDASU_SYNC_LINE.md)

> **位置づけ（v3 改訂中）:** 2026-06-29 提督方向 — **Notion タイムライン（表+棒）クローン** · **現場単位** · 最終 **全現場ポートフォリオ**。正本は [`SYNC_SCHEDULE_PRODUCT_DECISION.md`](SYNC_SCHEDULE_PRODUCT_DECISION.md) v3。以下 §1–§11 の一部は **v2（Excel 連動表）** の記述が残る。

> **旧 v2 一行要約:** ガント SaaS ではない · 直列連動表 · A3 · xlsx · ¥200/月。

---

## 1. プロダクト概要

| 項目 | 内容 |
|------|------|
| 製品名（案） | **SUGUDASU Sync Schedule** |
| URL（案） | `sync.sugudasu.com/schedule` |
| エンジン | `schedule-engine.js`（新規 · DOM 非依存） |
| 課金 | **¥200/月 · 1アカウント**（Stripe Subscription） |
| 提督 dogfood | 新規 SaaS リリースまでのマイルストーン（**約100行** · 日ベース） |

| やる（v1） | やらない（v1） |
|------------|----------------|
| 日ベース **または** 時間ベース（プロジェクト作成時に選択） | Excel Import |
| 親ID · 直列連動 · ロック行 · 限定CPスライド | 並行タスク · リソース山積み · 稼働平準化 |
| 可変列（テキスト/数値/選択） | Slack/LINE/メール自動アラート |
| A3 印刷 · xlsx Export | 変更履歴（DB は最新1版のみ） |
| 排他編集 · 閲覧URL | スマホ編集 |

---

## 2. 用語

| 用語 | 定義 |
|------|------|
| **プロジェクト（シート）** | 1つの工程表。日ベース or 時間ベースは **作成時に固定** |
| **モード · 日ベース** | 開始/終了は **日付**（稼働日カレンダー適用）。連動単位は **日** |
| **モード · 時間ベース** | 開始/終了は **日時**（分精度）。夜間作業・DB移行など **日付をまたぐ** 想定 |
| **親ID** | 行が追随する先行行の ID。指定された行だけが親の終端に連動 |
| **クリティカルパス（CP）** | 親IDの **終端** に、CP指定の **子行** の開始/終了が **元の期間分** スライド |
| **ロック** | チェック列。ON の行は **連動で動かない** |
| **稼働日** | プロジェクトごとの曜日設定 + 手動で追加した稼働日 |

---

## 3. データモデル

### 3-1. プロジェクト

```json
{
  "id": "proj_xxx",
  "mode": "day | datetime",
  "minutePrecision": true,
  "accountDefaultMode": "day",
  "workingWeekdays": [1, 2, 3, 4, 5],
  "extraWorkingDates": ["2026-06-28"],
  "frozenColumnCount": 3,
  "columns": [],
  "rows": []
}
```

| フィールド | 説明 |
|------------|------|
| `mode` | **`day` or `datetime`** — 作成時選択 · シート内混在不可（SCH-F03） |
| `workingWeekdays` | 0=日 … 6=土。非稼働は日数カウントからスキップ（SCH-A03） |
| `extraWorkingDates` | 途中で手動追加した稼働日（ISO 日付） |

**アカウント既定（SCH-A01）:** 新規作成時のデフォルトモードを **アカウント設定に保存**。プロジェクト作成時に上書き可。

### 3-2. 列定義

```json
{
  "id": "col_assignee",
  "label": "担当",
  "kind": "text | number | select",
  "options": ["A", "B"],
  "widthPx": 120,
  "system": false
}
```

| システム列（固定 · 左Frozen） | 説明 |
|------------------------------|------|
| タスク名 | 必須 |
| 開始 | 日 or 日時（mode 依存） |
| 終了 | 日 or 日時 |
| 所要 | 日数 or 分（mode 依存 · 算出可） |
| 親ID | 追随先行 ID（空=直列のみ） |
| CP | クリティカルパス子として親終端にスライド（bool） |
| ロック | チェックボックス |

ユーザー列: **無制限追加** · kind は **テキスト / 数値 / 選択** の3種（SCH-C01）。

### 3-3. 行

```json
{
  "id": "row_001",
  "cells": { "col_assignee": "山田" },
  "start": "2026-06-26",
  "end": "2026-06-28",
  "duration": 2,
  "parentId": "row_000",
  "criticalPath": false,
  "locked": false
}
```

時間ベース例: `"start": "2026-06-26T22:00"`, `"end": "2026-06-27T06:00"`（夜間・日跨ぎ · SCH-A04）

---

## 4. 連動ルール

### 4-1. 直列（SCH-B02）

- **並行タスク概念なし**。表は **上から下** が基本の直列連鎖。
- **親ID 未指定** の行: 直上の非ロック行の **終了** の翌稼働境界から **開始** が決まる（日ベースは翌稼働日、時間ベースは終了直後）。

### 4-2. 親ID（SCH-B01）

- `parentId` を持つ行は、**親行の終了** に追随して **開始/終了を再計算**（自行の **所要** は維持が既定）。
- **CP（criticalPath）= true** かつ親IDあり: 親の **終端変更** に応じ、子の開始/終了を **元の期間分** まとめてスライド。

### 4-3. ロック（SCH-B04）

- `locked: true` の行は **連動計算で移動しない**。
- ロック行の **手動編集** は許可。下流への伝播は §4-4・§5 に従う。

### 4-4. 行挿入（SCH-B03）

- 挿入時 **開始/終了は空欄**。
- 提督が日付/日時を入れた **瞬間** に連動エンジン実行。

### 4-5. 稼働日（SCH-A03）

- プロジェクトごとに **稼働/非稼働曜日** を固定指定。
- **extraWorkingDates** で途中から稼働日を追加可能。
- 日ベースの「+N日」は **稼働日のみカウント**（土日祝スキップ · 手動稼働日はカウント）。

### 4-6. 時間精度（SCH-A02）

- 時間ベースは **分単位**（秒は切り捨て表示）。

---

## 5. 終了日の前倒し・後ろ倒し（SCH-C02 · 提督回答）

**採用方針: 「編集したセルが何の意味か」で分岐**

| ユーザーが編集したもの | 挙動 |
|------------------------|------|
| **所要**（日数/分） | 終了を再計算 → **下流・親ID追随・CP子** を連鎖 |
| **開始** | 所要維持で終了を再計算 → 同上 |
| **終了**を直接変更 | **終了を確定値（ピン）** とみなし、**所要 = 終了 − 開始** を逆算 → 下流・追随を連鎖 |
| **ロック ON** | 当該行は連動で動かさない。手動変更のみ |

**原則:** 直列表なので「終了を前倒し」= **所要が短くなる** か **開始が繰り上がる** のどちらかが起き、**その差分が下流に伝播**する。Excel の「終了だけ触って式が壊れる」を、エンジン側で **所要の逆算** により吸収する。

**UI ヒント（v1 最小）:** 終了列を直接編集した直後、トースト1行「終了を基準に所要を更新しました」（過剰ダイアログは出さない）。

---

## 6. UI / 編集（SCH-D · SCH-C）

| 項目 | 仕様 |
|------|------|
| 入力 | **完全手動** — 「空の1行」追加から打ち込み（SCH-D02） |
| 行順 | **Notion風** — 行端 DnD · **スラッシュコマンド**（SCH-D01） |
| 左固定 | タスク名・開始・終了を **Frozen**、右は横スクロール（SCH-C03） |
| ソート | **なし** — **フィルタ**（担当者等で抽出表示）で代替（SCH-C04） |
| Undo | ブラウザメモリ **直近10回** のみ（SCH-D03） |
| スマホ | **閲覧のみ** · 編集不可（SCH-D04） |
| A3 ガイド | 画面上に **A3 枠の点線** を常時薄表示（SCH-E04） |
| UI 参照 | **Agent 必須** [`DESIGN_NOTION_SUGUDASU_ADAPT.md`](DESIGN_NOTION_SUGUDASU_ADAPT.md) → [`DESIGN_GUIDELINE_NOTION_LIKE.md`](../DESIGN_GUIDELINE_NOTION_LIKE.md) · `.cursor/rules/sugudasu-design-schedule.mdc` |
| 排他ボタン | **編集開始** · **保存** · **編集終了** — §9-1（終了≠保存） |

---

## 7. A3 印刷（SCH-E）

| 項目 | 仕様 |
|------|------|
| 用紙 | **A3 横**（既定 · `SYNC_SCHEDULE_PRODUCT_DECISION`） |
| 列選択 | 印刷前に **印刷する列** をチェックボックスで選択（SCH-E01） |
| ヘッダ | **全ページ最上部** にテーブルヘッダ（列名）繰り返し（SCH-E02） |
| ゼブラ | 印刷時 **1行おき薄グレー** 強制（SCH-E03） |
| 経路 | **UI 範囲印刷** または **xlsx 出力後に Excel 印刷**（SCH-G03） |

CSS 目安: `@page { size: A3 landscape; margin: … }` · `thead { display: table-header-group; }`

---

## 8. xlsx Export（SCH-F）

| 項目 | 仕様 |
|------|------|
| 値 | **すべて文字列**（例: `'2026/06/26` · 先頭 `'` 相当のテキストセル）（SCH-F01） |
| 数式 | **入れない** — サーバ/クライアントで **計算済み静的値** のみ（SCH-F02） |
| 日/時間混在 | **原理的に不可** — プロジェクト `mode` で単一（SCH-F03） |
| 色 | **背景 Fill のみ** RGB 再現（SCH-F04） |

---

## 9. Sync · 課金 · 上限（SCH-G）

| 項目 | 値 |
|------|-----|
| 価格 | **¥200/月 · 1アカウント** |
| プロジェクト数 | **最大 5** / アカウント（SCH-G01） |
| 行数 | **最大 1000 行** / プロジェクト（SCH-G01） |
| 日ベース跨度 | 実務上 **最大約3年**（1000行・稼働日カウントで足りる） |
| 超過 | 「サーバーの尊厳維持のため、これ以上の行追加をロックします（**LIMIT-SSOT**）」（SCH-G02） |
| 排他 | **1編集者** — 編集中は他ユーザー **読み取り専用**（SCH-G04） |
| 排他 UI | **明示ボタン** — 「編集開始」/「保存」/「編集終了」（提督 · 2026-06-26 · **B案**） |
| オフライン | **localStorage** 一時保存 → オンライン復帰で同期。複数端末時は **編集権は1端末**（SCH-H03） |
| 履歴 | **v1 なし** — DB は **最新状態1つのみ**（SCH-H04） |

### 9-1. 編集開始 · 保存 · 編集終了（同義ではない）

| 操作 | 意味 | サーバー | 排他ロック |
|------|------|----------|------------|
| **編集開始** | この端末が編集モードに入る | ロック取得 API | **取得** — 他者は閲覧のみ |
| **保存** | いまの表を DB に書き込む | **PUT 最新1版** | **維持**（編集セッション継続） |
| **編集終了** | 編集モードを抜ける | **未保存なら先に保存を促す**（下記） | **解放** — 他者が編集開始可 |

**編集終了 ≠ 保存（同義にしない）**

- **保存だけ** — 長時間編集中に途中経過をサーバーへ残す。**ロックは握ったまま**。
- **編集終了** — **ロック解除が主目的**。未保存変更があるときは  
  `保存して終了` / `保存せず終了（破棄）` / `キャンセル` の3択（v1 最小）。

**編集終了だけ押して DB が更新されない** 状態を許すと、他者が古い版を見たまま編集開始できるため、**既定は「保存して終了」を推奨ボタン**にする。

**閲覧者:** 編集開始ボタンは disabled。「〇〇が編集中（閲覧のみ）」表示。

**オフライン（SCH-H03）:** 編集開始中は localStorage にも書く。オンライン復帰時 **保存** でサーバーへ。編集終了時も未同期があれば保存を促す。

**タイムアウト（v1.1 検討）:** 編集開始のまま放置 → ロック自動解放は **v1 では任意**（提督が要らなければ出さない）。

---

## 10. MVP 受け入れ（チェックリスト）

- [ ] 新規プロジェクト: 日ベース / 時間ベース選択 · アカウント既定記憶
- [ ] 稼働曜日 + 手動稼働日追加 · 日数カウントに反映
- [ ] 時間ベース: 分精度 · 日跨ぎ夜間（開始日時・終了日時）
- [ ] 親ID追随 · CP子の期間維持スライド
- [ ] 直列連動 · ロック行 · 空欄挿入→日付入力で連動
- [ ] 終了直接編集 → 所要逆算 → 下流連鎖
- [ ] 列3種 · 無制限追加 · 左Frozen · フィルタ
- [ ] DnD · スラッシュ · Undo10 · スマホ閲覧のみ
- [ ] 印刷: 列選択 · ヘッダ繰返 · ゼブラ · A3ガイド
- [ ] xlsx Export: 文字列 · 静的値 · Fill色
- [ ] 5プロジェクト / 1000行 / LIMIT-SSOT · 排他（編集開始/保存/終了）· ¥200/月

---

## 11. OUT（v1 明示）

| ID | 内容 |
|----|------|
| SCH-H01 | リソース山積み · 稼働平準化自動調整 · Slack/LINE/メール自動アラート |
| — | Excel Import · 並行タスク · 変更履歴 · スマホ編集 |
| — | ガント横棒 DnD · 汎用クリティカルパスエンジン（**親ID+CPフラグの限定形のみ**） |

---

## 12. 提督回答ログ（原文索引）

| Q-ID | 要約 |
|------|------|
| SCH-A01〜04 | モード選択・アカウント保存 · 分精度 · 稼働曜日+手動稼働日 · 日時+夜間 |
| SCH-B01〜04 | 親ID · 直列 · 空欄挿入 · ロック列 |
| SCH-C01〜04 | 列3種 · 終了編集→§5 · Frozen · フィルタ代替ソート |
| SCH-D01〜04 | Notion UI · 手動入力行 · Undo10 · スマホ閲覧のみ |
| SCH-E01〜04 | 印刷列選択 · ヘッダ繰返 · ゼブラ · A3ガイド |
| SCH-F01〜04 | 文字列xlsx · 静的値 · モード単一 · Fill RGB |
| SCH-G01〜04 | 5PJ/1000行 · LIMIT-SSOT · Excel or UI印刷 · 排他 |
| SCH-H01〜04 | OUT通知系 · dogfood100行 · offline LS · 履歴なし |

---

## 14. OSS 参照 · タイムライン補助ビュー · 親子チャート色（2026-06-29）

**PoC:** `tmp/schedule-gantt-poc/` — **凍結**（親ブロック別 frappe は Notion UX と不一致）。次 PoC: **分割ペイン自前**（`SYNC_SCHEDULE_PRODUCT_DECISION.md` §5）

### 14-1. OSS 利用方針（v1 不変 + v2 補助）

| # | 決定 |
|---|------|
| 1 | **v1 主ビューは Notion 風表のみ** — ガント横棒ビューは v2 · 既定 OFF |
| 2 | **データ正本は `columns[]` + `rows[]`** — ガント OSS の task モデルに合わせない |
| 3 | **OSS は描画・スタイル参考まで** — 連動ロジックは `schedule-engine.js` のみ |
| 4 | **Notion UX（DnD · `/` · Frozen · フィルタ）はカスタム** — DHTMLX grid 等を v1 主 UI にしない |
| 5 | **名乗り・SEO** — 「工程表」「連動スケジュール」。ガント SaaS と名乗らない |

**v2 候補ライブラリ:** [frappe-gantt](https://github.com/frappe/gantt)（MIT · 読み取り専用ミラー）· DHTMLX CE は v1 には重い · ganttrify は R/静的のみ（A3 見た目参考）

### 14-2. 親チャート · 子チャート（タイムライン層 · v2 想定）

表データとは **別の表示階層**（正本は引き続き §3 の行モデル）。タイムライン補助ビューで次を満たす。

| 項目 | 仕様 |
|------|------|
| **親チャート** | **無制限**（実装はソフト警告 · Sync cap は §9 既存） |
| **子チャート** | 各親に **無制限**紐づけ · `parentChartId` で所属 |
| **描画** | 親ごとに 1 ブロック — 先頭行=親サマリ棒 · 続く行=子タスク（frappe-gantt 1 インスタンス/親） |
| **編集** | v2 PoC 段階は **表示のみ**（v1 OUT: ガント横棒 DnD を維持） |
| **日付** | 親の start/end は **子の min/max から自動算出**（手動上書きは v2.1 検討） |

```json
{
  "parentCharts": [
    { "id": "pc_1", "name": "設計フェーズ", "color": "#2563EB" }
  ],
  "childCharts": [
    { "id": "cc_1", "parentChartId": "pc_1", "name": "ワイヤー", "start": "2026-07-01", "end": "2026-07-05" }
  ]
}
```

表の **親ID**（行連動）と **親チャート**（表示グループ）は **別概念** — 同名でも ID 空間を分ける（`row_*` vs `pc_*` / `cc_*`）。

### 14-3. 色 — 親系統 · 子シェード · ユーザー指定

| 項目 | 仕様 |
|------|------|
| **既定パレット** | SUGUDASU 寄り 6 色（`DESIGN_NOTION_SUGUDASU_ADAPT` 準拠）— 青 `#2563EB` · 緑 `#059669` · 橙 `#EA580C` · 紫 `#7C3AED` · 赤 `#E11D48` · 灰 `#64748B` |
| **子の色** | 親色の **同一色相（H）** · 明度 L を子の並び順で段階的に上げる（青親 → 青系トーン） |
| **ユーザー指定** | 親ごとに **HEX / `rgb()` / `rgba()`** を保存可 · 子は個別上書き可（未指定時は親系統シェード） |
| **白 · 透過** | `#FFFFFF` · `#RRGGBB00` · `transparent` · `rgba(…,0)` — **fill 透過** · 判別用に **stroke（hairline）のみ** 表示 |
| **xlsx** | §8 既存 — Fill RGB のみ再現 · 透過は **無 Fill** 相当 |

**実装ユーティリティ（PoC）:** `tmp/schedule-gantt-poc/lib/colors.mjs` — 本番は `schedule-gantt-colors.js` へ昇格検討。

---

## 15. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-06-26 | 提督回答より v1 SSOT 初版 · SCH-C02 方針を §5 に確定 |
| 2026-06-26 | §9-1 排他 B案 — 編集開始/保存/編集終了（同義にしない） |
| 2026-06-26 | 提督合意 — v1 正本として着手可 · 実装・dogfood 後の改訂は本ファイル changelog に追記 |
| 2026-06-29 | **§14** — OSS 方針 · 親子チャート階層 · 色体系 · PoC `tmp/schedule-gantt-poc/` |
