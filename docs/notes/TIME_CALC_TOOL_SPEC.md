# 時給計算 — 仕様SSOT（time-calc · 別Agent引き継ぎ用）

**更新:** 2026-07-10（productName **SUGUDASU 時給計算** に確定）  
**リポジトリ:** `C:\asl_dev\sugudasu`  
**ステータス:** **実装済 · alpha**

> **別Agentへ:** [`data-finance-priority-gemini-RESULT.md`](data-finance-priority-gemini-RESULT.md) P1-6 · `data/roadmap.json` `time-calc-labor`

---

## 1. プロダクト概要

| 項目 | 内容 |
|------|------|
| ファイル | `tools/time-calc.html`（URL `/time-calc`） |
| registry `id` | `time-calc` |
| 命名 | productName **SUGUDASU 時給計算** · navLabel **時給計算** · conceptName **時給計算** |
| 一言 | **勤務時間の h:mm を合計し、時給で換算**（非送信） |
| ペルソナ | アルバイト · フリーランス · 店舗マネージャー |
| **やらないこと** | 給与計算（社保・税金・手取り）· 残業代・割増の自動判定 |

**id 命名:** `time-calc` を維持（URL 破壊しない）。表示名だけ「時給計算」。  
**DECISION:** 「給与計算」は過大訴求のため不採用。

---

## 2. UI · 入出力

### 入力

- 複数行テキストエリア（コピペ）または行追加 UI
- 時給（円）フィールド

### パース優先順位

1. `h:mm` / `hh:mm`（例 `1:30` `02:45`）
2. 10進数（例 `1.5` → 1時間30分）
3. 日本語（例 `90分` `1時間30分`）

不正行は `0:00` またはスキップ + 薄い警告。

### 出力

- **総労働時間:** `58:30` 形式（**24時間超えも累計** — Excel `[h]:mm` 罠の回避）
- **10進時間:** 例 `58.5時間`
- **概算支給額:** 時給 × 総分（端数 **切捨て** 明示）

---

## 3. やらないこと（F7）

- 法定労働時間 · 残業代 · 深夜割増の **自動判定 Out**
- 勤怠 SaaS 代替 · シフト保存
- 確定申告根拠としての保証

**免責:** 簡易概算用 · 給与明細の代替ではない · 要専門家確認。

---

## 4. 上限

- **31〜60行**（1か月分シフト想定）

---

## 5. 非送信

「給与や勤務時間のデータはサーバーに一切送信されません」

---

## 6. 実装チェックリスト（Q1'27）

- [ ] `tools/time-calc.html` + パース/合計モジュール + テスト
- [ ] `tool-registry.json` · hub · changelog
- [ ] FAQ: Excel 24h リセット · 端数 · 残業 Out

---

*End of SSOT*
