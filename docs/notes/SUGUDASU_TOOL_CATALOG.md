# SUGUDASU ツールカタログ

**更新:** 2026-07-24  
**対象:** ユーザーが利用できる実務ツール37本（案内・法務・内部ページは除外）  
**命名正本:** `data/tool-registry.json`  
**説明文参照:** `data/hub-cards.json` · `docs/products/{id}/`

> 「ツール名」は `productName`、「ID名」はURL・ファイル・コードで使う識別子です。

| ツール名 | ID名 | 何をするツールか・ユーザーの何を解決するか | SUGUDASUならではの工夫 |
|---|---|---|---|
| SUGUDASU 請求書 | `invoice` | 見積書・納品書・請求書を作成し、PDF印刷する。消費税や源泉税の計算、書類の作り直しの手間を減らす。 | 見積から請求への転用、内税8%・10%分離、源泉10.21%、A4複数枚、端末内の下書きに対応。 |
| SUGUDASU 電子印鑑 | `stamp` | 苗字や日付を入力して、書類に使う認印画像を作る。印影画像を別途用意する手間をなくす。 | 認印をブラウザ内で生成し、請求書ツールへそのまま渡せる。角印は手持ち画像の利用にも対応。 |
| SUGUDASU 領収書 | `receipt` | 金額・宛名・但し書きを入力して領収書を作成・印刷する。手書きや表計算ソフトでの整形を減らす。 | 手取り額から金額を逆引きでき、A4への複数面付けやURL共有にも対応。 |
| SUGUDASU ラベル | `label` | 住所リストを宛名ラベルの枠へ配置して印刷する。大量発送時の転記・位置合わせを楽にする。 | 一括貼り付け時のヘッダー行除外や姓名スペース保護により、表データをそのまま扱いやすい。 |
| SUGUDASU シフト | `shift` | スタッフの勤務予定を組み、月間シフト表と労働時間を確認する。偏りや手作業の集計を減らす。 | 公平性を考慮した生成、確定枠のロック、印刷時の改ページなど、現場運用を前提にしている。 |
| SUGUDASU 議事録 | `report` | 箇条書きの会議メモを、決定事項・課題・次のタスクが分かる議事録や報告書へ整える。 | AIへ送らず、決まった整形ルールだけでブラウザ内処理する。内容を勝手に補完しない。 |
| SUGUDASU 逆引き | `reverse` | 意味やニュアンスから類語・言い換え表現を探す。言葉が思い出せない、表現が単調という困りごとを解決する。 | 単なる五十音辞典ではなく、用途やニュアンスから逆引きできる実務向けの構成。 |
| SUGUDASU 全角半角整え | `normalize` | コピペ文の全角半角、余計な空白・改行、日付、区切りなどを一括で整える。表記ゆれの手直しを減らす。 | 行整理、伏字、SQL IN形式、タブ→カンマなど、事務作業で実際に起きる「あと一手」をプリセット化。 |
| SUGUDASU 仮置き | `clip-stash` | 複数ツールを行き来する途中で「あとで使う」コピーを一旦置く。メモ帳・Excel・チャットへの退避を減らす。 | Text/Table/URL/Image/Color の5種のみ。編集・整理・タグなし。Ctrl+V→ダブルクリックで戻す IndexedDB 退避。 |
| SUGUDASU 検索式ビルダー | `search-query` | AND・OR・除外・`site:`・`filetype:`を組み合わせたGoogle検索式を作る。検索構文を覚える負担を減らす。 | 入力内容を送信せず検索式だけを組み立て、完成後にGoogle検索へ移れる。 |
| SUGUDASU 表変換 | `table-conv` | Excel貼り付け・CSV・TSVの表をMarkdownやJSONなどへ変換する。形式ごとの手作業を減らす。 | 最大5,000行、Shift-JIS、複数形式の相互変換をブラウザ内でまとめて扱う。 |
| SUGUDASU WebP変換 | `webp-to-jpg` | WebP画像をJPGまたはPNGへ一括変換する。非対応アプリで画像を開けない問題を解決する。 | 画像をアップロードせず端末内で変換し、複数画像もまとめて処理できる。 |
| SUGUDASU 動画コマ抜き | `video-frame` | MP4・WebM動画の任意の瞬間を静止画PNGとして取り出す。スクリーンショットのタイミング調整を楽にする。 | 動画を送信せず、ブラウザ上で再生位置を見ながら必要な1コマを抜き出す。 |
| SUGUDASU 赤入れ | `annotate` | 提出・共有前に、必要な場所だけ整える。隠す·囲む·指す。Slack/Teams向けにコピーして共有。 | Canvas端末内。黒塗り/ぼかし/モザイク + 矢印/枠/楕円。PDFは編集ページのみ画像化。PDF記入（完成）とは別。旧 `mask`。 |
| SUGUDASU 画像切り出し | `image-trim` | 画像を指定サイズや縦横比でトリミングする。SNSや資料用のサイズ合わせを簡単にする。 | 切り出し枠を固定して画像側を動かす操作、用途別プリセット、クリップボード貼り付けに対応。 |
| SUGUDASU 余白トリム | `clipboard-trim` | コピー画像やスクリーンショットの周囲にある不要な余白を自動で除去する。資料へ貼る前の切り抜きを省く。 | `Ctrl+V`で貼るだけで余白を判定し、トリム後のPNGをそのままコピーできる。 |
| SUGUDASU 透かし | `watermark` | 複数画像へ文字やロゴの透かしを入れる。転載対策や出所表示の反復作業を減らす。 | 9位置・透過度・文字/ロゴを端末内で一括処理し、まとめてZIP出力できる。 |
| SUGUDASU PDF記入 | `pdf-fill` | 提出用PDFに文字・印鑑・黒塗りを足して完成させる。印刷→手書き→スキャンの回り道を減らす。 | Acrobat代替ではなく提出専用。焼き付けは端末内ラスタ。既存文字編集はしない。 |
| SUGUDASU PDF画像抽出 | `pdf-images` | PDF内に埋め込まれた画像を取り出す。スクリーンショットによる画質劣化や1枚ずつの作業を避ける。 | ページの画像化ではなく埋め込み画像そのものを抽出し、複数画像をZIPで持ち帰れる。 |
| SUGUDASU テストデータ | `test-data` | 開発・検証用の氏名、住所、社員情報などのダミーデータをCSV・JSONで生成する。実データを使うリスクを減らす。 | 社員マスタを最大25万件まで生成でき、IDオフセットや日付形式を実務システムに合わせられる。 |
| SUGUDASU 壊れ入力 | `broken-input` | 長文、多言語、絵文字、見えない文字、制御文字など異常系テスト用文字列をコピーする。フォームの弱点確認を速くする。 | ランダム生成ではなく、壊れやすい入力を目的別に固定カタログ化。見た目と実際のコピー値を分けている。 |
| SUGUDASU 班分け | `group-split` | 参加者を条件に合わせて複数の班へ振り分ける。研修・イベントの班分けと手直しを減らす。 | 条件を緩めるモード、タップ再編、最大250名、セッションJSON、PCからスマホへ渡す同期メモを備える。 |
| SUGUDASU ドラフト会議 | `match-board` | 第1〜第3希望と定員を見ながら、部署・委員会などへの割り当てを調整する。複雑な希望調整を見える化する。 | 長時間の調整を前提にUndo/RedoとJSON再開を備え、希望順位と定員を同じボードで扱う。 |
| SUGUDASU 枠取りパレット | `slot-board` | 人や仕事を定員付きの枠へドラッグして配置する。枠超過や未配置を見落としにくくする。 | 超過を赤表示し、復元コードとTSVで結果を持ち帰れる、制限付きカンバン。 |
| SUGUDASU 見積会議 | `planning-poker` | 開発タスクの見積もりを全員で伏せて入力し、同時公開する。声の大きい人への同調や認識差の見逃しを減らす。 | 専用アカウントや各自端末なしでも、司会者の1画面で全員分を伏せて入力・Revealできる。 |
| SUGUDASU イベント進行 | `timeline` | イベントの進行表を作り、時間変更に合わせて後続予定を再計算する。押し巻き時の修正漏れを減らす。 | ±5分調整、途中差し込み、連動再計算、TSVコピーを備え、現場での急な変更に寄せている。 |
| SUGUDASU 抽選 | `fair-draw` | 名簿から公平に当選者を抽選し、キャンペーン景品の法的リスクを一次チェックする。抽選後の説明負担も減らす。 | シード付き抽選、名簿スナップショット、監査PDF/JSONの証跡3点セット、Connpass CSV取込を備える。 |
| SUGUDASU 引き算パレット | `budget-trim` | 項目ごとの金額を増減し、合計を予算上限内へ収める。どこを削ればよいかの試行錯誤を楽にする。 | ±1万・10万円の即時調整、超過表示、ハッシュ共有、TSV出力に絞り、複雑なグラフを置かない。 |
| SUGUDASU 割り勘 | `warikan` | 役職やグループごとに傾斜をつけ、端数を揃えて割り勘する。集金額と会計額のズレを防ぐ。 | 固定額を先に引いて残りを按分し、丸め後の過不足まで合わせ、精算用テキストを出力する。 |
| SUGUDASU SNS | `sns` | SNSプロフィールや投稿向けに、文字を装飾文字・花文字へ変換する。コピー可能な見た目の違いを試せる。 | 変換だけでなく、お手本とプレビューから選べる。Unicodeの欠番を補正し、文字化けしにくくしている。 |
| SUGUDASU リンク集QR | `link-qr` | 複数URLを1つのリンク集とQRコードにまとめる。イベント後のSNS・連絡先交換を短時間で済ませる。 | 入力したリンク集をURLのフラグメントに収め、サーバー保存やアカウントなしで共有できる。 |
| SUGUDASU QR読取 | `qr-reader` | カメラ、画像、貼り付け、画面共有からQRコードを読み取る。別端末を用意できない場面にも対応する。 | 同じPC画面上のQRも画面共有で読め、リンク集QR形式なら内容をカードとして見やすく表示する。 |
| SUGUDASU 差分チェック | `diff` | 修正前後の文章や契約書を比較し、追加・削除・変更を色分けする。数字や日付など重要変更の見落としを減らす。 | 単なる文字差分ではなく、危険度の高い変更を優先表示し、コピー・印刷まで非保存で完結する。 |
| SUGUDASU JSON構造 | `json-view` | APIレスポンスやログのJSONをツリーで確認し、キー・値を探して場所や値をコピーする。深い構造で迷う問題を解決する。 | 「貼る・見る・探す・コピー」に限定し、常時見えるPathBarから人間向けの場所確認とJSONPathコピーができる。 |
| SUGUDASU AIコピペ整形 | `ai-cleaner` | AI出力をNotionやCursorへ貼る前に、Markdown、コードブロック、JSONの崩れを整える。コピペ後の掃除を減らす。 | AIで意味を解釈せず、決定的なルールだけで端末内整形するため、文章内容を勝手に書き換えない。 |
| SUGUDASU 時給計算 | `time-calc` | 複数日の開始・終了・休憩から勤務時間を合計し、時給換算の目安を出す。電卓での日別集計を減らす。 | `1015`のようなコロンなし入力、入力候補の再利用、10進小数表示に対応し、「給与計算」とは明確に分ける。 |
| SUGUDASU フォント変換 | `font-converter` | 入力文字をSNSなどで使えるUnicode装飾文字や記号へ変換する。好みの字体をコピーして使える。 | 23種類のフォントスタイルと444記号を収録し、SNSお手本中心の`/sns`とは役割を分けている。 |

## 対象外

次はツールではなく、案内・運用・法務・内部確認ページのため表から除外しています。

- `hub`（ツール一覧）
- `updates`（更新履歴）
- `roadmap`（開発ロードマップ）
- `statements`（SUGUDASU の約束）
- `privacy`（プライバシーポリシー）
- `terms`（利用規約）
- `disclaimer`（免責事項）
- `not-a-car`（名称混同防止ページ）
- `brand-logo-preview`（内部ロゴプレビュー）
- `present`（**Reject** · 2026-07-24 · ギフト提案 · Hub/カタログ除外 · [`CASE-2026-006`](../legal/CASE_LAW.md#case-2026-006)）
