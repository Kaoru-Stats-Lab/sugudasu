# SUGUDASU PDF画像抽出 — 製品仕様 SSOT（v0.1）

**更新:** 2026-07-18  
**ステータス:** **v0.1 実装済**（`tools/pdf-images.html`）  
**実装詳細:** [`PDF_IMAGE_EXTRACT_TECH.md`](PDF_IMAGE_EXTRACT_TECH.md)（pdf.js 内部API · 定数 · 参考実装の落とし穴）  
**Ledger:** X07 **GO（条件付き）**  
**仮 id:** `pdf-images` · URL `/pdf-images`（命名は PLAYBOOK で最終確認）  
**関連:** watermark（抽出後の透かし）· mask / image-trim  

> **一言:** 仕様書・資料PDFから **埋め込み画像だけ** を端末内で取り出し、保存する。ページの見た目コピーでも、PDF圧縮でも、OCRでもない。非送信。

---

## 0. 外部レビュー反映（GPT · Claude）

| 指摘 | 判定 | 反映 |
|------|------|------|
| SSOTから実装APIを分離 | **採用** | 本ファイル＝製品。TECH＝実装 |
| 参考実装の固有名・内部APIをSSOTから除去 | **採用** | TECH の「参考実装の問題点」へ |
| 境界・0件・色空間の期待値を明示 | **採用** | §1 · §4 |
| 小画像フィルタを 32px 硬直から緩和 | **採用** | 既定は短辺 **16px未満** を除外（トグルなし） |
| JPEGは「維持できるときだけ」 | **採用** | §2 出力 |
| 上限は目安＋**超過は拒否**（先頭だけ処理しない） | **採用（両者折衷）** | §2 · 数値は TECH 定数 |
| DedupはDL用。UIは出現ページを併記 | **採用（Claude）** | §2 · §3 |
| ファイル名にページ番号 | **採用** | §2 Must |
| 抽出件数の明示 | **採用** | §3 UI |
| メモリ枯渇の説明文言 | **採用** | §4 |

**総合:** 製品GOを維持。本改訂で「SSOT確定なのに未決」を解消。

---

## 1. 製品境界（Job）

| 向く | 向かない |
|------|----------|
| 仕様書・提案書から **図・スクショ・写真** を抜いてスライド / チャットへ | OCR・テキスト抽出 |
| 社外秘PDFを **アップロードせず** 手元で抜く | PDF圧縮・結合・分割・墨消し |
| **埋め込み画像がある** PDF | ベクターのみ・文字のみ（**抽出0件になりうる**） |

**重要（境界）:**  
PDFの種類によっては画像が埋め込まれておらず、**1枚も抽出できない**ことがあります。また、見た目が画像でも内部が図形描画だけの場合は対象外です。

**ユーザー向け一言:**  
「埋め込まれた画像を端末内で取り出します。ページ全体の見た目コピーではありません。PDFファイル自体はサーバーに送りません。」

**導線:**

```text
PDF → 本ツールで画像抽出 →（任意）透かし → 保存
```

---

## 2. v0.1 Must / OUT

### Must

| 項目 | 内容 |
|------|------|
| 入力 | PDF **1ファイル** · ドロップ / クリック |
| 上限超過 | **拒否**（先頭Nページだけの部分処理はしない）。目安値は TECH 定数 |
| 抽出対象 | PDFに **埋め込まれた画像**（ページ全体ラスタ化ではない） |
| 出力形式 | **自動選択**（元データをJPEGのまま維持できる場合は JPEG。それ以外は PNG）。UIで形式バッジを表示 |
| ファイル名 | `{元ベース名}_p{ページゼロ埋め2桁}_img{連番ゼロ埋め2桁}.{png\|jpg}` 例: `sample_p03_img02.png` |
| 保存 | 単枚DL · **ZIP一括**（watermark の ZIP 実装を流用可） |
| UI | サムネ一覧 · **「N件抽出」を常時表示** · 各サムネに **出現ページ**（例: p.3 · p.3, p.5）· **JPEG/PNG バッジ** |
| Dedup | **ダウンロード用バイトの重複を防ぐ**。一覧では同一画像でも出現ページを併記して「消えた」誤解を防ぐ |
| 小画像 | 既定で **短辺 16px未満 かつ 面積 256px²未満** を除外。トグルは v0.1 OUT。FAQで説明 |
| 非送信 | `data-sg-privacy-badge` · `data-subject="PDF"` |
| PC | Desktop-first バナー必須 |
| エンジン読込 | **当該ツールのみ**動的読込（サイト全体に載せない） |
| 依存 | 公式 PDF エンジン（Mozilla pdf.js / `pdfjs-dist`）のみ。追加ラッパーパッケージは v0.1 に入れない |

### OUT（v0.1）

- ページ全体を画像化（**v0.2 候補**）  
- PDF圧縮・結合・分割・墨消し・OCR  
- 複数PDF一括 · フォルダ階層  
- 抽出と同時の透かし焼き込み（完了後に `/watermark` へ誘導）  
- 小画像フィルタのUIトグル  
- CMYK / ICC / ソフトマスク / Form内ネストの完全対応（ベストエフォート · 期待値は §4）  

---

## 3. UI（必須体験）

1. PDFを選ぶ  
2. 処理後、見出し級で **「18件抽出」** のように件数を出す（0件も明示）  
3. サムネ一覧：プレビュー · ファイル名候補 · **出現ページ**  
4. 単枚保存 / ZIP  
5. 空・上限・色の注意は画面上または FAQ で読める  

---

## 4. 失敗・注意のユーザー文言

| 状況 | 文言（要旨） |
|------|----------------|
| 画像0件 | 「埋め込み画像は見つかりませんでした。文字・図形だけのPDFや、画像が別の持ち方の場合があります。」 |
| 小画像除外 | 「ごく小さい画像（短辺16px未満かつ面積がごく小さいもの）は除外しています。」 |
| 上限超過 | 「このPDFは目安の上限を超えているため処理できません。（部分的な抽出はしません）」 |
| 読込失敗 | 「このPDFは開けませんでした（暗号化・破損の可能性）。」 |
| 色空間 | 「一部の画像は色が正しく出ないことがあります（印刷向けの色空間など）。」 |
| メモリ等 | 「処理中に止まった場合は、ページの少ないPDFや画像の少ないPDFで再度お試しください。」 |
| 見た目はあるが0件 | 「ページに図が見えても、埋め込み画像ではなく図形として描かれている場合があります。その場合は抽出できません。」 |

---

## 5. 命名（仮）

| 層 | 仮 |
|----|-----|
| id | `pdf-images` |
| conceptName | PDF画像抽出 |
| productName | SUGUDASU PDF画像抽出 |
| navLabel | PDF画像 |

---

## 6. 受け入れ（DoD）

1. 埋め込み画像付きサンプルから、目視で分かる図が1枚以上出る  
2. 一覧の件数表示が結果と一致する。同一画像の出現ページが併記される  
3. 上限超過時は **拒否**（一部だけ成功しない）  
4. 出力ファイル名が §2 の形式に従う  
5. PDFの内容がサーバーへ送信されない（ページ読込・解析等の通信は約束ページ方針どおり別）  
6. Desktop バナー · privacy-badge · FAQ（0件・図形描画・色・小画像）  
7. TECH 記載の単体テストが exit 0  

---

## 7. v0.2（今はやらない）

- **ページ全体の画像化は別ツール候補**（Jobが違うため本ツールにモード同居しない）  
- 小画像フィルタのトグル · **除外件数の表示**  
- Form 内ネストの強化  

---

## 8. 1行サマリ

**埋め込み画像抽出に全振りした製品として GO。実装詳細は TECH。上限超過は拒否。ページ全体化と圧縮はやらない。**
